# React + Node.js + MongoDB (Full Stack Course)

Repository นี้รวบรวมโค้ดทั้งหมดที่ใช้ประกอบการเรียน  
คอร์ส **React + Node.js + MongoDB (Full Stack)**  
โดยใช้งานควบคู่กับเอกสารประกอบการเรียนจาก Google Drive

---

## 📌 โครงสร้างโปรเจกต์

- โค้ดจะแยกตามบทเรียน (Lesson / Chapter)
- แต่ละโฟลเดอร์เป็นโปรเจกต์แยกกัน
- สามารถเข้าไปทดลองรันได้ทีละบทเรียน

---

## ⚙️ วิธีติดตั้งและใช้งานโปรเจกต์

### 1. เข้าไปยังโฟลเดอร์ของบทเรียน

```bash
cd <ชื่อโฟลเดอร์บทเรียน>
```

---

### 2. ติดตั้ง Dependencies

รันคำสั่งต่อไปนี้เพื่อดาวน์โหลด Library และ Module ที่จำเป็น

```bash
npm install
```

คำสั่งนี้จะติดตั้งแพ็กเกจทั้งหมดที่ระบุไว้ในไฟล์

```bash
package.json
```

โดยอัตโนมัติ

---
